#!/bin/bash

local script_path=$(cd $(dirname $0); pwd)
source "${script_path}/shell"
source "${script_path}/osx"

# source "${script_path}/peco"
source "${script_path}/file_manager"
source "${script_path}/fzf"
source "${script_path}/package"
